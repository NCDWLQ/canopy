{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows the desktop shell to use core Tauri APIs. SQL remains Rust-only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
